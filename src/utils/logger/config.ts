export enum AllowedNodeEnvs {
    production = 'production',
    uat = 'uat',
    staging = 'staging',
    dev = 'dev',
    tests = 'tests',
}

export enum AllowedLogLevels {
    ERROR = 'ERROR',
    WARN = 'WARN',
    INFO = 'INFO',
    HTTP = 'HTTP',
    VERBOSE = 'VERBOSE',
    DEBUG = 'DEBUG',
    SILLY = 'SILLY',
}
